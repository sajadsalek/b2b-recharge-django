services:
  db:
    image: postgres:17-alpine
    restart: always

    environment:
      - POSTGRES_DB=recharger
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data


volumes:
  pgdata: